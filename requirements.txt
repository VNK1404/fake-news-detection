# ──────────────────────────────────────────────────────────────
# requirements.txt — Fake News Detection Module
# AI-Driven Digital Evidence Integrity Monitoring System
# ──────────────────────────────────────────────────────────────

# Web Interface
flask>=3.1.0

# OCR
pytesseract>=0.3.10
opencv-python>=4.8.0

# PDF Extraction
pdfplumber>=0.10.3

# HTTP Requests
requests>=2.31.0

# ──────────────────────────────────────────────────────────────
# Phase 1 — RoBERTa ML Classifier
# ──────────────────────────────────────────────────────────────

# Deep learning framework (CPU by default; for GPU install torch with CUDA)
torch>=2.0.0

# HuggingFace model hub + tokenizer + sequence classification
transformers>=4.40.0

# HuggingFace datasets (used in train.py for Dataset / DatasetDict)
datasets>=2.18.0

# HuggingFace evaluate (used in train.py for metric computation)
evaluate>=0.4.1

# Scikit-learn — accuracy, precision, recall, F1 in _compute_metrics
scikit-learn>=1.3.0

# Required by HuggingFace Trainer for distributed / mixed-precision training
accelerate>=0.28.0

# Numerical helpers (usually already installed via torch/sklearn)
numpy>=1.24.0

# ──────────────────────────────────────────────────────────────
# Phase 2 — FAISS Claim Similarity Search
# ──────────────────────────────────────────────────────────────

# Sentence embedding model (all-MiniLM-L6-v2, ~80 MB)
sentence-transformers>=2.7.0

# Facebook AI Similarity Search — CPU build
faiss-cpu>=1.7.4
